import 'package:flutter/material.dart';

class AMRTextFormField extends StatelessWidget {
  const AMRTextFormField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return TextFormField();
  }
}
